node_modules
.next
out
next-env.d.ts
*.tsbuildinfo
.DS_Store
.env*.local

# Supabase CLI runtime state
supabase/.temp/

# Local Claude Code state (launch.json is intentionally tracked)
.claude/settings.local.json
.claude/*.lock
.vercel

# Isolated implementation workspaces
.worktrees/

# Minted worker credentials. scripts/mint-worker-token.mjs writes these into the
# working directory; one was committed to this public repository on 2026-08-29.
# Never commit them -- they authenticate a site worker to the control plane.
worker-token-*.jwt
*.jwt
