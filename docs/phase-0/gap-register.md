# Phase 0 — Gap Register: Guild-A

**Survey date:** 2026-08-07. Every gap below was observed directly against the
live cluster (see `site-inventory.md` for the underlying evidence) and checked
against a specific Master Plan commitment. Severity reflects blast radius if
GuildCloud launched on this infrastructure exactly as found today.

| ID | Gap | Plan reference | Evidence | Severity |
| --- | --- | --- | --- | --- |
| **G-01** | ~~Tailscale ACL is fully open~~ — **partially addressed 2026-08-07**: `tag:guildcloud-mgmt`/`tag:operator` zone scaffolding added and applied, 16 devices tagged, policy moved under GitOps (`infra/tailscale/`, see Decision Record). Pre-existing broad owner grants deliberately kept (no real customer identity exists yet to isolate from). **Tenant-zone isolation still does not exist** — no `tag:guildcloud-tenant-*` grant has been created, since there's no real tenant to scope it to. Re-open in severity once Phase 3 needs it. **Drift noticed 2026-08-07 (during G-05 work), fixed 2026-08-08: nodeA had lost its `tag:guildcloud-mgmt` tag** (nodeB–E still had it) — confirmed still drifted a day later (not a transient blip), then re-tagged via direct device-tag call (device tagging isn't part of the `policy.hujson` GitOps flow, which governs grants only) and verified live. Root cause still unconfirmed — same open question as the `podC` tag reversion noted earlier; worth a real investigation if a third device does this. | §6 "Project policy determines which identities may reach which resources"; §3 boundary "no public VPS IP or public SSH" | `manage_acl` → `grants` block; see `docs/decisions/2026-08-07-tailscale-tenancy-model.md` | Was **Critical** → now **Medium** (scaffolding exists; enforcement for real tenants is Phase 3 work) |
| **G-02** | ~~No backups exist~~ — **resolved (on-site) 2026-08-07**: PBS deployed (VM 400, nodeE, separate local disk from `ceph-vm`), daily backup job scheduled cluster-wide (7-day retention), and a real restore drill run and proven (backup → restore to new VM → booted → hostname and machine-id confirmed matching). See `docs/decisions/2026-08-07-backup-architecture.md`. **Real bug found and fixed 2026-08-08**: a Phase-0-verification pass (before starting Phase 1) checked actual recent scheduled-job history, not just the original restore drill — found every nightly run since setup had been silently failing at the prune step (`missing Datastore.Modify|Datastore.Prune`, the same token/user privilege-separation gotcha already documented once, missed on a second grant). Backup data itself was always fine; retention was never actually enforced. Fixed by granting `DatastorePowerUser` to the underlying `backup@pbs` user; verified with real `vzdump` runs on both clusters afterward (`TASK OK`, prune succeeded). See `docs/decisions/2026-08-08-backup-prune-permission-fix.md`. **REOPENED same day, 2026-08-08 evening**: a pre-Phase-2 verification pass ran fresh real `vzdump` backups again — VM 111's prune step failed with the identical `missing Datastore.Modify|Datastore.Prune` error, live. The grant is not durably in effect (unclear whether it never fully persisted or was reverted by something since). Could not inspect PBS's own ACL state directly this session — `execute_vm_command` against the PBS VM (400) returned empty output for every command tried, and no other available tool reaches PBS's own API (only Proxmox VE). Needs direct PBS shell/dashboard access to re-check and re-apply the grant, and to investigate why it didn't hold. See `docs/dev-log/2026-08-08-backup-prune-regression-found.md`. **Off-site/two-location backup remains open** — blocked on gap G-13 (single site), not fixable by configuration. | §8 entire section; §3 Standard tier "daily encrypted off-site backup" | `cluster/backup` lists both clusters' jobs; restore drill in Decision Records; prune regression reproduced live 2026-08-08 evening | Was **Critical** → **Medium** (2026-08-08 AM) → **reopened, back to High** (2026-08-08 PM): retention is not reliably enforced, on-site backup data transfer itself still confirmed working; off-site blocked on G-13 |
| **G-03** | No HA resources configured — a node failure does not restart or relocate any guest. **Re-scoped 2026-08-07**: the plan's exact text (§10) explicitly defers automatic failover — *"MVP automation safely retries jobs and reconnects; operators handle real incidents. Advanced automatic failover comes later."* Recovery at MVP is backup/restore (G-02), not live HA. See `docs/decisions/2026-08-07-g03-ha-deferred.md`. | §10 "Advanced automatic failover comes later"; §3 "No untested SLA or active-active promise" (previously mis-cited as "implicit in §8 'restore into a healthy site'" — that line is a backup/restore promise, not live HA) | `cluster/ha/resources` → `[]` | Was **High** → now **Low** (post-MVP hardening item, not a Phase 0 blocker) |
| **G-04** | No control-plane, API, auth, or data model exists — every console screen is mock data with no persistence | §13 Phase 1 (Control plane foundation) | `grep` for API routes/DB/auth deps in the console repo: none found | **High** (expected at this stage, but blocks everything downstream) |
| **G-05** | ~~No datacenter firewall rules defined~~ — **partially addressed 2026-08-07**: cluster-wide firewall enabled (policy explicitly ACCEPT/ACCEPT/ACCEPT, matching prior real behavior — no default-deny), with a targeted DROP rule on every node blocking the 7 pre-existing legacy-workload guest IPs (`+legacy-workloads` IPSet) from reaching Proxmox management ports (8006, 22). Verified: all nodes online/quorate, Ceph `HEALTH_OK`, all guests unaffected. See `docs/decisions/2026-08-07-g05-firewall-proposal.md`. **Full §6 zone-based firewall design still not done** — blocked on G-16 (Cisco switch unreachable, VLAN work incomplete). | §6 private-access boundary | `cluster/firewall/rules` (DC level) → still `[]` by design (rules are per-node); see node-level rules for the real state | Was **High** → now **Medium** (the concrete lateral-movement risk is mitigated; full zone model remains blocked on G-16) |
| **G-06** | Standing root SSH grant to 7 external Gmail accounts on `tag:gean-devnet` hosts. **Reviewed 2026-08-07 — explicit decision: keep as-is.** No longer a silent carry-forward. | §10 "no automatic support access... any exceptional access is customer-approved, time-limited, audited, revocable" — this grant still doesn't meet that bar, but retaining it was a stated choice, not an oversight | ACL `ssh` block, third entry; decision logged in `docs/decisions/2026-08-07-tailscale-tenancy-model.md` | Still **High** — the grant itself is unchanged, only its status moved from unnoticed to acknowledged |
| **G-07** | `homeassistant` advertises `0.0.0.0/0`, `::/0`, and the LAN subnet as exit-node routes | §6 zone model — nothing should offer a default route into tenant traffic | Tailscale device list, `advertisedRoutes` | Medium (routes are advertised but **not** approved — `enabledRoutes` is empty, so not yet in effect) |
| **G-08** | Six stale test/template Tailscale registrations remain enrolled (`guildct-template`, `guildct-template-1`, `ct-clone-test`, `ts-autojoin-test`, `ts-autojoin-ct-test`, `agent-watch-test`). **Attempted 2026-08-08**: user approved removal, but the `device_action(delete)` tool call was refused by the MCP tool permission layer itself ("requires admin risk level") — nothing was deleted, verified via a fresh device-list re-read. Needs either removal via the Tailscale admin console directly, or this session's Tailscale MCP connection granted admin-level device permissions. | Housekeeping; widens G-01's exposed surface | Tailscale device list, last seen 2026-07-27 to 07-31, re-confirmed still present 2026-08-08 | Medium |
| **G-09** | ~~Ceph `.mgr` pool runs `size=2, min_size=2`~~ — **resolved 2026-08-08**: bumped to `size=3` (matching `ceph-vm` and `k8s-rbd`, both already `size=3` on this same 4-OSD cluster, proving the capacity is safe). Task completed `OK`, verified via a fresh pool read (`size: 3` confirmed) and cluster Ceph status (`HEALTH_OK`, 65 PGs, no degraded/misplaced objects before or after). | §8 "keep encrypted copies across at least two locations" implies redundancy margin, not a single-fault-tolerant floor | `ceph/pool` listing, before/after | Was **Medium** → now resolved |
| **G-10** | ~~OS template catalogue has Ubuntu 26.04 and Debian 13 only~~ — **resolved 2026-08-08**: sourced current (verified live via real directory listings, not guessed — training data doesn't know 2026-08 release versions) GenericCloud images for Fedora 43, Rocky Linux 10.2, AlmaLinux 10.2. Built VMIDs 9003/9004/9005, matching the exact existing Ubuntu/Debian template pattern (ceph-vm storage, cloud-init, Tailscale vendor snippet, agent enabled), all verified via fresh config reads. Guild-A's catalogue now covers all 5 OSes in §7. **Not covered**: per-template lifecycle management (version tracking, security updates, deprecation policy per §7) doesn't exist for any of the 5 templates, old or new. Guild-B's own catalogue wasn't touched. See `docs/decisions/2026-08-08-g10-template-catalogue.md`. | §7 "Ubuntu (recommended), Debian, Fedora, Rocky Linux, and AlmaLinux" | All 5 templates confirmed via live guest-list query and individual config reads (`template:1`, correct storage/tags) | Was **Medium** → now resolved (catalogue gap only — lifecycle management is a separate, still-open need) |
| **G-11** | ~~EVPN SDN zone and two VNets are configured but zero guests use them — the tenancy mechanism is unvalidated~~ — **actually validated 2026-08-08** with real test guests (not just config review): two throwaway VMs attached to `vnet50`/`vnet60`, tested, then fully destroyed. **Finding 1**: intra-zone routing works (gateway + exit-node reachable). **Finding 2 (new gap, fix attempted, NOT resolved)**: external SNAT egress is broken — despite `snat:1` configured, traffic reaches the exit node (`nodeA`) but never gets NAT'd to the real LAN/internet (0% to both `192.168.8.1` and `1.1.1.1`). Predates today's G-05 firewall work. Hypothesis tested: the Proxmox API documents SNAT as firewall-dependent ("enable masquerade for this subnet if pve-firewall"), and this zone predates the firewall being enabled — triggered a full SDN reload (`PUT /cluster/sdn`, completed `OK`, cluster/Ceph verified healthy after) to regenerate the rule. **Re-tested with a fresh throwaway VM — still 100% broken, identical symptom.** Hypothesis ruled out. Root cause remains undiagnosed — needs host-level netfilter/FRR inspection this session has no tooling for (guest-agent exec only reaches guest VMs, not the Proxmox host). **Finding 3 (architectural)**: `vnet50` and `vnet60` are **not isolated from each other** — expected Proxmox SDN behavior (`exitnodes-local-routing:1` routes between all VNets sharing one zone/VRF), but means the current setup is a connectivity demo, not a tenancy model. Real per-tenant isolation needs one zone per tenant, a design decision not yet made. See `docs/decisions/2026-08-08-g11-sdn-validation.md`. | §16 "Tailscale tenancy model must be validated"; also the VLAN/IPAM half of that same decision | Real connectivity tests via two throwaway guests (created, tested, destroyed — zero footprint left) | Was **Medium** → validation done, but surfaced 2 new real gaps: broken SNAT egress (needs its own fix) and no actual tenant isolation (needs its own design decision) |
| **G-12** | ~~No monitoring is wired to the cluster~~ — **substantially corrected 2026-08-08**: while investigating Guild-B (G-13/G-21), found a real, pre-existing monitoring stack on Guild-B's Kubernetes cluster — `kube-prometheus-stack` (Prometheus, Grafana, node-exporters), Loki+promtail for logs, and custom `proxmox-exporter` deployments explicitly labeled for **both** `pve_cluster=guild-a` and `pve_cluster=guild-b` (built ~13 days before this discovery, unrelated to this session's work). `uptime-kuma` (the host referenced in the original gap) also lives on Guild-B (`podC`) as a separate, simpler uptime/ping monitor — its "relationship to Guild-A" is now fully confirmed: same platform, complementary purpose. **However, live-verified as currently non-functional**: Prometheus and Grafana are both single-replica with no anti-affinity, both scheduled on `k8s-w-1` (which lives on the still-offline `podE`) — confirmed via a direct health-check to Prometheus's own ClusterIP returning connection-refused, not just stale pod-status metadata. This is a real, ongoing outage of the monitoring stack itself, a live consequence of the `podE` deferral (see G-19), not a hypothetical. Even once healthy, this stack only covers the Proxmox/cluster/log layer — none of §10's product-layer surface (private access, backup success/failure, billing, status page) exists yet, since Phase 1 hasn't started. | §10 Monitoring and alerts | `kubectl get pods/nodes -n monitoring`; live health-check to Prometheus ClusterIP (`code=000`, connection refused) | Was **Medium** → still **Medium**: infrastructure exists but is currently down; scope is also narrower than §10 requires even when healthy |
| **G-13** | ~~Only one site exists~~ — **major correction 2026-08-08**: a second real, quorate, 5-node Proxmox cluster (`Guild-B`) exists and was missed entirely by the original 2026-08-07 survey, which only ever queried the `guild-a` cluster in the MCP tool despite it supporting multi-cluster access. Found via an unrelated monitoring investigation (G-12). User confirmed Guild-B is intended as GuildCloud's second site. **However, it does not give real site redundancy today** — it's on the exact same LAN and gateway (`192.168.8.1`) as Guild-A, i.e. the same physical location, same router, same power. A router/power/site-wide failure takes out both simultaneously. Warm Standby still has no *geographically* separate target. See `docs/phase-0/site-inventory-guildb.md` and `docs/decisions/2026-08-08-guildb-discovery.md`. | §3 Warm Standby tier; §8 restore | `pve_list_clusters` → `guild-a (default)`, `guild-b`; full survey in `site-inventory-guildb.md` | Was **Medium** → still open, re-scoped: the "no second cluster" problem is solved, the "no geographically separate site" problem is not |
| **G-14** | Non-GuildCloud workloads (mediastack, coolify, jellyfin, rabbitmq, irc, pdm-datacenter, ingress, proxmox-mcp) occupy real capacity on the same nodes. **Policy decided 2026-08-07**: temporary occupants, not permanent overhead — they stay running for now (no forced migration today) but must move to separate hardware before any real capacity/pricing commitment is published, or this decision gets explicitly revisited. Actual measured footprint: ~10.08 GB RAM used / ~22.53 GB configured (excl. `proxmox-mcp`). See `docs/decisions/2026-08-07-g14-legacy-workload-policy.md`. | §11 "plans and quotas are derived from measured real capacity" | Guest inventory cross-referenced against capacity model | Still **High** — stays open until migrated or the decision is revised; no longer *undecided*, which is the part that was blocking |
| **G-15** | nodeC has half the RAM of every other node (8.21 GB vs 16.65 GB); nodeE contributes no Ceph OSD | §16 "measured mini-PC... capacity" | Node inventory | Low-Medium — a placement/scheduling input, not a blocker |
| **G-16** | Physical switch (Cisco, per §6) was not reachable through any available API — its VLAN/port configuration is unverified | §6 physical site model; §16 network design prerequisite | Not observable via Proxmox or Tailscale APIs | Medium — blocks finishing G-11's VLAN design |
| **G-17** | ~~No performance benchmark has been run~~ — **resolved 2026-08-08**: real measurements via two throwaway VMs (created, benchmarked, fully destroyed). **Provisioning**: ~110-120s clone→agent-ready. **Storage** (fio, 4K random 70/30 on the same `ceph-vm` pool every guest uses): 139 read / 62 write IOPS, write latency avg 10.87ms (p99 24ms), disk 98.59% utilized during test — modest numbers, consistent with a small home-lab Ceph cluster, not enterprise storage. **Network** (nc+dd, inter-node): ~9.2MB/s (~73.6Mbps) sustained — lower than expected, not root-caused (raced a concurrent dist-upgrade on the same guest; worth a clean re-test). A live illustration found by accident: a kernel dist-upgrade on the benchmark guest was visibly slowed by exactly the write throughput just measured — real infrastructure, real consequence. See `docs/decisions/2026-08-08-g17-benchmark.md`. | §16 "measured provisioning and recovery performance on actual sites before any customer expectation is stated" | Real fio/nc+dd measurements, two independent provisioning-time data points | Was **Medium** → resolved (numbers exist and are real; a cleaner uncontended network re-test and CPU/Guild-B benchmarks remain optional follow-ups) |

| **G-18** | ~~Guild-B has no backup jobs configured~~ — **mostly addressed 2026-08-08**: registered against the existing `guild-pbs` server (reused, not a new VM) under its own PBS **namespace** (`guild-b`) to avoid VMID collisions with Guild-A (both clusters independently assign overlapping VMIDs — 101, 102, 200, 300, 9000+ all collide; a shared flat datastore silently rejects or misattributes backups for colliding IDs). Daily job `guild-b-standard-daily` created (7-day retention, all guests), and a real proof backup was run and **succeeded** (`TASK OK`, 20GiB transferred, snapshot independently confirmed queryable in PBS) — not just configured. See decision record for the full story including a `backup owner check failed` bug hit and fixed. **Capacity risk resolved 2026-08-08**: measured `gean-devnet`'s actual disk usage (not nominal) — its 1TB disk has ~991.5GB of real, non-sparse data (a live Ethereum devnet), confirming it could never realistically fit regardless of PBS sizing. Grew the PBS VM's disk 100GB→200GB using confirmed-available thin-pool headroom on nodeE (no risk to other guests), then scoped the daily job to exclude the three large, pre-existing, non-GuildCloud guests (`gean-devnet` 300, `cloudstack-aio` 400, `fleetbase` 500 — same out-of-scope category as Guild-A's G-14 legacy workloads). Re-ran the actual job parameters (not just a single guest) to prove it: VM102's second backup used dirty-bitmap incrementals (120MiB transferred, 99% reused) confirming ongoing cost is cheap; datastore held steady at ~180GB available throughout. See `docs/decisions/2026-08-08-g18-pbs-capacity.md`. | §8 entire section, now applies to a second real cluster | PBS namespace + storage + job all verified live; proof backups run, capacity headroom confirmed empirically during a real multi-guest run | Was **Medium** → now resolved |
| **G-19** | ~~Guild-B has no datacenter firewall rules~~ — **closed 2026-08-08**: same design as G-05 (explicit ACCEPT default, targeted DROP for guest-workload IPs reaching Proxmox mgmt ports 8006/22). Enabled node-by-node (podB→podD→podC→podA, lowest-risk first) with reachability verification after each; cluster stayed quorate throughout, no Kuma monitors newly broke. `podE` is offline — its rules are configured but not yet enabled. **Deliberately deferred by the user 2026-08-08**, not an oversight — revisit when `podE` is back online: enable `nodes/podE/firewall/options` (`enable:1`) and re-verify reachability the same way podA-D were, per `docs/decisions/2026-08-08-g18-g19-guildb-wiring.md`'s methodology. See `docs/decisions/2026-08-08-g18-g19-guildb-wiring.md`. | §6 private-access boundary, now applies to a second real cluster | `nodes/{podA,podB,podC,podD}/firewall/rules` — all confirmed live and enabled | Was **High** → now resolved (podA-D); podE pending |
| **G-20** | Guild-B provides no real disaster-recovery value as a "second site" — it shares Guild-A's LAN, gateway, and (presumably) power and physical location. True Warm Standby needs geographic separation, which neither cluster provides. | §3 Warm Standby tier explicitly implies a separate location | Network config: both clusters gateway through `192.168.8.1` | Medium — doesn't block using Guild-B for capacity/placement diversity, but must not be marketed or architected as disaster-recovery-grade redundancy until a genuinely separate location exists |
| **G-21** | Guild-B runs a live Kubernetes cluster serving public-facing HTTPS domains (`argocd`, `coolify`, `grafana`, `headlamp`, `jellyfin`, `rabbitmq`, `requests`, `datacenter` — under `guild-technologies.com` now, `guildserver.io` has expired). **Critical sub-finding, mitigated 2026-08-08**: `headlamp.*` (a full Kubernetes dashboard) had **zero ingress-level auth** and ran in `-in-cluster` mode bound to `cluster-admin` — full unauthenticated cluster compromise if reachable. Downgraded the ClusterRoleBindings to read-only `view` and paused ArgoCD self-heal on that Application so the fix holds (it's GitOps-managed and would have silently reverted otherwise). **This is a mitigation, not a fix** — `view` still exposes cluster state to any unauthenticated visitor; proper OIDC auth or removing the public ingress is the real fix. **Remaining Guild-B services audited 2026-08-08**: Portainer's ServiceAccount is also bound to `cluster-admin`, ingress live on the *current* domain (`portainer.guild-technologies.com`) — deliberate config (`owner: david`), not a chart default. Gated by Portainer's own login (unlike Headlamp's zero-auth), so a compromise of that login = instant full cluster-admin. **User explicitly reviewed and accepted this risk as-is.** ArgoCD (broad `get/delete/patch` on `*`) and Grafana (read-only on all Secrets cluster-wide) are both standard, expected patterns for their respective tools, not flagged as misconfigurations. **Correction**: coolify/jellyfin/rabbitmq/requests/datacenter are NOT Guild-B k8s services (only 6 real ingresses exist: argocd/demo/headlamp/grafana/portainer/stateful) — they're Guild-A legacy workloads, reverse-proxied separately; that's a different investigation (app-level auth, not k8s RBAC) — see follow-up. See `docs/decisions/2026-08-08-g21-remaining-services-audit.md`. | §3 "No public VPS IP or public SSH in the MVP" | DNS resolution + Kuma monitor list + direct `kubectl` inspection via QEMU guest agent; live public reachability still not independently confirmed from this session | **Critical** (Headlamp sub-finding, now mitigated) — parent gap stays **High**: other services unreviewed, and the domain-migration status itself is unconfirmed |

## Immediate next actions this register implies

In the order the Master Plan's own §17 lists them:

1. **Close G-01 before anything else.** A private-by-default product running on
   a fully-open network is a false promise today, not a future one.
2. **Decide G-06** explicitly — keep, scope down, or revoke the external root
   SSH grant — rather than let it carry forward silently.
3. **Stand up backups (G-02)** before any customer data exists on this
   cluster, not after. (G-03/HA re-scoped 2026-08-07 — the plan defers
   automatic failover past MVP; see `docs/decisions/2026-08-07-g03-ha-deferred.md`.
   No longer in this priority path.)
4. ~~**Resolve G-14**~~ — **decided 2026-08-07**: pre-existing workloads are
   temporary, must migrate off before any real capacity/pricing commitment.
   See `docs/decisions/2026-08-07-g14-legacy-workload-policy.md`. No
   migration has happened yet — gap stays open until it does.
5. Everything else in this register can be scheduled into Phase 1/2 planning
   normally; none of it blocks starting the control-plane work in G-04.
